---
description: (codepatrol) Review the Plan of an explicit Change
---
Load `codepatrol-review` for `$ARGUMENTS`. Inspect the exact work id and branch,
write only `review/`, record elapsed/token coverage and choose `approve`,
`fix-first`, or `rework`. Stop without production edits or invoking Apply.
