---
description: (codepatrol) Plan a new project or feature into a reviewable artifact package
---
Load the `codepatrol-plan` skill via the skill tool and execute it for the following user intent:

$ARGUMENTS

Follow the skill's SKILL.md exactly. Produce the decision-complete `spec.md` and context-free `plan.md` under `docs/codepatrol/<work-id>/`, record the handoff, validate for review, and stop at `ready-for-review`. Do not implement production code.