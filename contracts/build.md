# Build

Implement the selected Plan inside the task's isolated workspace. Commit all
changes, leave the workspace clean, and submit a summary for every Work. CodePatrol
creates and anchors one canonical candidate commit from the final tree.

The harness loads resolved catalog instructions as the system prompt under the
common Agent Catalog contract.
Batch openings share one base commit; each returned task has its own workspace.
