# Plan

Produce one independent plan for every Work in a Wave. Open one or more options with
`--agents reference@version,...`, or use the configured
`agentCatalog.defaults.plan` selection when `--agents` is omitted. Every selected
agent receives the same context snapshot and its resolved system prompt for that
batch.
