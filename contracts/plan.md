# Plan

Produce one independent plan for every Work in a Wave. Open one or more options with
`--agents reference@version,...`; every selected agent receives the same context
snapshot and its resolved system prompt for that batch.
