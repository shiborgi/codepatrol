interface:
  display_name: "Codepatrol Status"
  short_description: "Render deterministic Change lifecycle status"
  default_prompt: "Use $codepatrol-status to render the deterministic Change Kanban and exact resume actions."
